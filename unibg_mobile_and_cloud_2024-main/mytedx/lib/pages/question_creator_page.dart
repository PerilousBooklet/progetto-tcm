import 'package:flutter/material.dart';

class QuestionCreatorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('TedQuiz'),
        ),
        body: Expanded(
          child: ListView(
            children: [],
          ),
        ));
  }
}
